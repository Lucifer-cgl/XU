{
  "name": "xu-knowledge-base",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "content:build": "node scripts/build-content.mjs",
    "dev": "npm run content:build && vite",
    "preview:local": "npm run content:build && vite --host 127.0.0.1 --open",
    "build": "npm run content:build && vite build && node scripts/build-workspace.mjs",
    "preview": "vite preview"
  },
  "dependencies": {
    "dompurify": "3.4.15",
    "katex": "0.16.22",
    "marked": "15.0.12",
    "marked-katex-extension": "5.1.10"
  },
  "devDependencies": {
    "vite": "6.4.3",
    "wrangler": "4.133.0"
  }
}
